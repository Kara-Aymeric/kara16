<?xml version="1.0"?>
<odoo>
    <record id="custom_crm_lead_view_form" model="ir.ui.view">
        <field name="name">custom.crm.lead.view.form</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page name="state_tracking" string="State tracking">
                    <field name="stage_change_delta_ids" readonly="1" force_save="1">
                        <tree delete="false">
                            <field name="date" />
                            <field name="old_stage_id" />
                            <field name="stage_id" />
                            <field name="delta" />
                            <field name="user_id" />
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
